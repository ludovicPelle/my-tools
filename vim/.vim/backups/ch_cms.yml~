all:
  theme:                aristo # default theme to use for ch_cms application
  use_db_preferences:   false                # do you want to use the chCms preferences stored in database 

  sf_guard_plugin:        #used to rerieve the user thaks to his mail and his username
    mail_table_name:      sfGuardProProfile
    mail_link_field_name: user_id
    mail_field_name:      mail
    auth_function:        [chCmsLogin,connect] # the signin function, returns a user if signin, false otherwise, accepts $username, $password as args

  widget:
    before: <div class="widget-homepage ui-corner-all"  id="%id%">
    before_header: <h2 class="widget-header ui-corner-top">
    after_header:  </h2><div class="widget-body">
    after:  </div></div>

  visitor:
    scope:   pro            # default is %your_app%
    enabled: true                   # so you could disable it
